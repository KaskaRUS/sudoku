<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="space.zhdanov.game.sudoku">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.Light">
        <meta-data android:name="io.sentry.dsn" android:value="https://d5a39fc070f3414da4cfb02edca9b2dc@o1148585.ingest.sentry.io/6220051" />
        <meta-data android:name="io.sentry.traces.sample-rate" android:value="1.0" />
        <activity
                android:name=".FinishGameActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_finish_game"
                android:theme="@style/Theme.AppCompat.Light.Fullscreen">
        </activity>
        <activity
                android:name=".MenuActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_menu"
                android:theme="@style/Theme.AppCompat.Light.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".GameActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_menu"
                android:theme="@style/Theme.AppCompat.Light.Fullscreen"/>
        <activity
                android:name=".TutorialActivity"
                android:configChanges="orientation|keyboardHidden|screenSize"
                android:label="@string/title_activity_menu"
                android:theme="@style/Theme.AppCompat.Light.Fullscreen"/>
    </application>

</manifest>